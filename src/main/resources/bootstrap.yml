spring:
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}
  application:
      name: spring-boot-docker
  datasource:
    driverClassName: com.mysql.jdbc.Driver
    url: jdbc:mysql://mysql:3306/spring-boot-docker?useUnicode=true&amp;characterEncoding=utf-8
    username: root
    password: 123456

server:
  port: 9876
